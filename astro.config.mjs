import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://rensschuil02.github.io',
  // Uncomment the line below if deploying to a repository named something other than <username>.github.io
  // base: '/portfolio-website-project',
});
